Классифицируй посты из списка ниже. Верни строго JSON массив — никакого текста до или после.
{{ content }}
Напоминание:
- Найди t.me/ ссылки в каждом посте. Не в белом списке — реклама (ad_rate >= 0.8).
- "Подписывайтесь" + чужая ссылка — реклама (ad_rate >= 0.9).
- Белый список каналов: {{ CHANNELS }}
- Белые плашки: PRANK_TG, Мы в MAX
- Тема поста не отменяет рекламу.
- Количество элементов в ответе = количеству входных постов.
Формат — строго JSON массив:
[
  {
    "id": "channel/12345",
    "ad_rate": 0.85,
    "proof_of_ad": "Одно предложение на русском, объясняющее решение."
  }
]